<div class = 'post-create-form' >
    <%= form_with url: polymorphic_path([current_user, Post.new]), scope: :post  do |f| %>
        <%= f.text_area :content %>
        <%= f.submit "Post" %>
    <% end %>
</div>
