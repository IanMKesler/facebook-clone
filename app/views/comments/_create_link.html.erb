<%= button_to polymorphic_path([current_user, commentable, Comment.new], action: 'new'), {id: "#{commentable.id}-new-comment-link", class: "btn btn-white btn-xs" , method: :get, remote: true} do %>
    <i class="fa fa-comments"></i> Comment
<% end %>